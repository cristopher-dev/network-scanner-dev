import React from 'react';
import { Paper, Typography } from '@mui/material';

const DeviceGrouping: React.FC = () => {
  return (
    <Paper sx={{ p: 2 }}>
      <Typography variant="h6">Agrupación de Dispositivos</Typography>
      {/* Aquí iría la agrupación de dispositivos */}
    </Paper>
  );
};

export default DeviceGrouping;
